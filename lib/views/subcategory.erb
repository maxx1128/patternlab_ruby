<% @subcategory_data.each do |item| %>

  <%= erb :'includes/pattern_embed', :locals => {
    :header => "h3",
    :label => item["label"],
    :file_href => "#{@lvl1}/#{@title.downcase}/#{item["label"]}",
    :pattern_href => true
  } %>

<% end %>

